# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

variables:
   projectFolder: 'website'
   buildFolder: 'website/build'
   qualityGateThreshold: '21'

stages:
- stage: Development
  pool:
    vmImage: ubuntu-latest
  jobs:
  - job: Development
    steps:
    - script: cd $(projectFolder) && npm install
      displayName: 'Install dependencies'
    - script: cd $(projectFolder) && npm run build
      displayName: 'Build'
    - script: cd $(projectFolder) && npm test -- --coverage --coverageThreshold='{\"global\":{\"branches\":\"$(qualityGateThreshold)\",\"functions\":\"$(qualityGateThreshold)\",\"lines\":\"$(qualityGateThreshold)\",\"statements\":\"$(qualityGateThreshold)\"}}'
      displayName: 'Test'
    - task: ArchiveFiles@2
      inputs:
        rootFolderOrFile: $(buildFolder)
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/release.zip'
        includeRootFolder: true
    - task: PublishBuildArtifacts@1
      inputs:
        pathtoPublish: '$(Build.ArtifactStagingDirectory)/release.zip'

- stage: QA
  dependsOn: Development
  condition: succeeded('Development')
  pool:
    vmImage: ubuntu-latest
    environment: Experimental environment
  jobs:
  - job: Placeholder
    steps:
    - script: echo "Hello World"
